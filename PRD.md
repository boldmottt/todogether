# PRD — todogether

**버전**: 0.1 (초안)
**최종 수정**: 2026-06-23
**플랫폼**: iOS 17+
**스택**: SwiftUI · SwiftData · WidgetKit · CloudKit

---

## 1. 제품 개요

### 1.1 한 줄 정의

같이 사는 사람, 자주 만나는 사람과 할 일을 함께 관리하는 공유 투두 앱.

### 1.2 핵심 가치 제안

기존 투두 앱은 개인용이거나, 공유가 되더라도 단순한 리스트 공유에 그친다. todogether는 세 가지로 차별화한다.

1. **공유방(Space)**: 관계 단위로 투두를 묶는다. 가족방, 룸메이트방, 커플방처럼 관계마다 별도 공간을 만들고, 멤버가 같은 투두를 보고 완료할 수 있다.
2. **연계형 투두**: "A가 끝나야 B가 활성화"되는 의존 관계를 설정할 수 있다. 분업할 때 순서를 강제할 수 있고, 자신의 차례가 오면 알림을 받는다.
3. **위젯 중심 UX**: 앱을 열지 않아도 홈 화면 위젯에서 투두를 완료할 수 있다. 오늘 할 일을 위젯에서 바로 체크하는 것이 주 사용 패턴이다.

### 1.3 타겟 사용자

| 페르소나 | 특징 | 주요 사용 시나리오 |
|----------|------|------------------|
| 커플 / 동거인 | 집안일 분담이 필요한 2인 | 청소 당번, 장보기 목록 공유 |
| 소규모 가족 | 자녀 학사 일정, 부모님과 일정 조율 | 병원 예약 → 약 구매 체인 |
| 친한 친구 그룹 | 여행 준비, 이벤트 준비 | 여행 체크리스트 템플릿 공유 |
| 개인 사용자 | 루틴 관리, 습관 트래킹 | 완료 후 N일 반복 루틴 |

### 1.4 포함하지 않는 것 (v1.0)

- 프로젝트 관리 기능 (간트차트, 타임라인)
- 10인 이상 대규모 팀
- 안드로이드 / 웹
- 유료 구독 / 인앱 결제
- AI 기반 자동 추천

---

## 2. 사용자 문제 정의

### 2.1 현재 사용자들이 겪는 문제

**문제 1 — 공유가 안 되거나 불편하다**
카카오톡 채팅으로 "장봐줘", "청소 언제 해?"를 주고받다 흐지부지된다. 기억에 의존하거나 메모장을 캡처해서 보내는 방식은 지속되지 않는다.

**문제 2 — 순서가 있는 분업을 표현할 수 없다**
"내가 재료 사면 네가 요리해"처럼 순서가 있는 분업은 기존 앱으로 표현할 수 없다. 결국 직접 연락해서 전달해야 한다.

**문제 3 — 투두 앱은 자주 열지 않게 된다**
앱을 열어야 확인할 수 있으면 결국 안 쓰게 된다. 오늘 할 일이 홈 화면에 있어야 한다.

**문제 4 — 반복 루틴 설정이 딱딱하다**
"매주 월요일"은 되지만 "운동하고 나서 3일 뒤에 다시"는 안 된다. 완료 시점 기준 반복이 안 되기 때문에 습관형 투두에 맞지 않는다.

### 2.2 문제-기능 매핑

| 문제 | 해결 기능 |
|------|----------|
| 공유가 불편 | 공유방(Space) + CloudKit 실시간 sync |
| 순서 있는 분업 | 연계형 투두 (prerequisite) |
| 앱 안 열게 됨 | 인터랙티브 홈 위젯 |
| 반복 루틴 한계 | afterCompletion 반복 모드 |

---

## 3. 기능 요구사항

### 3.1 공유방 (Space)

#### 개념
- Space = 관계 단위의 투두 컨테이너. iCloud 계정 기반 CloudKit 공유.
- 각 Space는 고유한 색상을 가지고, 피드/캘린더에서 출처를 색상으로 구분한다.

#### 기능 목록

**SF-01 Space 생성**
- 이름 입력 + 색상 선택(팔레트 6색)으로 Space 생성
- 생성 직후 초대 링크 발행 옵션 제공
- 내 Space 목록 최대 제한 없음 (실용적으로 ~10개)

**SF-02 초대**
- `UICloudSharingController`를 통해 AirDrop / 메시지 / 링크 복사로 초대
- 초대 링크는 iCloud 계정이 있어야 수락 가능
- 수락 전까지 "초대 대기 중" 상태 표시

**SF-03 권한**
- Owner: 투두 CRUD, 멤버 초대, Space 삭제
- Member: 투두 완료/미완료, 댓글(v2), 탈퇴 가능
- Owner는 Member를 강퇴할 수 있음

**SF-04 실시간 Sync**
- 멤버 중 누군가 투두를 완료하면 다른 멤버 기기에 즉시 반영
- 오프라인 상태에서 완료 → 온라인 복구 시 자동 sync
- 충돌 시: 마지막 쓰기 우선 (Last Write Wins)

**SF-05 Space 삭제 / 탈퇴**
- Owner가 삭제 → 모든 멤버에서 Space와 투두 제거
- Member가 탈퇴 → 해당 멤버의 기기에서만 제거, 다른 멤버에겐 유지

#### 엣지 케이스
- iCloud 미로그인 사용자: Space 기능 잠금, 로그인 유도 배너 표시
- 초대 링크 만료: CloudKit 기본 정책 따름 (별도 만료 없음)

---

### 3.2 투두 (Todo)

#### 기본 속성

| 필드 | 타입 | 설명 |
|------|------|------|
| 제목 | String | 필수, 최대 200자 |
| 메모 | String? | 선택, 최대 2000자 |
| 마감일 | Date? | 날짜만 / 날짜+시간 선택 |
| 담당자 | iCloud User? | Space 내 멤버 중 선택. 미지정 시 "누구나" |
| 상태 | locked / available / completed | 연계 상태 |
| 반복 | RecurrenceRule? | 섹션 3.4 |
| 공유방 | Space? | nil이면 내 투두 |

#### 기능 목록

**TF-01 투두 생성**
- 빠른 추가: 텍스트만 입력 후 Enter (마감일, 담당자 없이)
- 상세 생성: 마감일, 담당자, 반복, 선행 조건 설정 가능
- 어느 뷰에서든 하단 플로팅 버튼으로 생성 가능

**TF-02 완료 처리**
- 스와이프 또는 체크버튼으로 완료
- 완료 시: completedAt 기록, status → completed
- 반복 규칙 있으면 즉시 다음 투두 생성
- 연계형 투두 있으면 후속 투두 잠금 해제 + 담당자 알림

**TF-03 수정 / 삭제**
- 제목 인라인 수정 (길게 탭)
- 상세 편집 시트에서 전체 필드 수정
- 삭제: 스와이프 → 확인 없이 삭제 + Undo 토스트 (5초)
- 완료된 투두는 별도 "완료됨" 섹션에 최근 7일치 표시

**TF-04 정렬 / 필터**
- 기본 정렬: 마감일 오름차순 (미지정은 하단)
- 담당자 필터: 내 투두 / 전체
- 완료 숨기기 토글

**TF-05 연계형 투두 (차별 기능)**
- 투두 상세에서 "이 투두 먼저 완료해야 함" 선행 조건 추가
- 선행 조건: 같은 Space 내 다른 투두 선택 (다중 선택 가능)
- 선행이 미완료이면 status = locked → 체크버튼 비활성화
- 선행이 모두 완료되면 status = available + 담당자에게 로컬 알림 "이제 [제목] 할 수 있어요"
- 사이클 감지: A→B→A 같은 순환 참조 설정 시 에러 표시

**잠긴 투두 시각 표현**
- 체크버튼 자리에 자물쇠 아이콘
- 제목 텍스트 회색
- 탭하면 "선행 투두: [제목]" 표시 (무엇을 기다리는지)

---

### 3.3 템플릿 (Template)

#### 개념
반복되는 투두 묶음을 저장하고, 기준일을 지정해 한 번에 생성.

#### 기능 목록

**TPL-01 템플릿 생성**
- 이름 + 항목 리스트
- 각 항목에 지정 가능: 상대적 마감일(기준일 +N일), 선행 조건(항목 인덱스)
- 드래그로 순서 변경, 스와이프로 삭제

**TPL-02 템플릿 사용**
- 기준일(오늘 / 날짜 선택) 입력
- 대상 Space 선택 (내 투두 / 특정 공유방)
- "생성" → 투두 N개 일괄 생성

**TPL-03 템플릿 관리**
- 자주 쓰는 템플릿 상단 고정 (핀)
- 중복 (복제)
- 삭제 (생성된 투두는 유지)

**기본 제공 템플릿** (앱 최초 설치 시 예시)
- 주간 집안일
- 여행 준비
- 이사 체크리스트

---

### 3.4 반복 (Recurrence)

#### 반복 모드

| 모드 | 설명 | 예시 |
|------|------|------|
| 매일 | 매 N일 | 매일, 2일마다 |
| 매주 | 매 N주 (요일 선택 가능) | 매주 월/수/금 |
| 매월 | 매 N개월 (일 선택) | 매월 1일 |
| 매년 | 매 N년 | 매년 생일 |
| 완료 후 N일 | 완료 시점 기준 재생성 | 완료 후 3일 (습관형) |

#### 동작

- 완료 처리 시 즉시 다음 투두 생성 (별도 스케줄러 없음)
- 다음 투두는 원본과 동일한 제목, 담당자, 반복 규칙, 선행 조건 상속
- 반복 중단: 해당 투두의 반복 설정 제거 (이후 생성 안 됨)
- 종료일 설정 가능 (특정 날짜 이후 생성 안 됨)

---

### 3.5 위젯 (Widget)

#### 크기별 사양

**Small (2×2)**
- 가장 급한 투두 1개
- 제목 + 마감일
- 완료 토글 버튼
- 탭 → 앱 해당 투두 상세로 딥링크

**Medium (4×2)**
- 오늘 마감 / 마감 없는 투두 최대 4개 리스트
- 각 행에 완료 토글
- 잠긴 투두는 자물쇠 아이콘으로 표시 (탭 안 됨)
- 빈 상태: "오늘 할 일 없음 🎉"

**Large (4×4)**
- 오늘 + 내일 투두 최대 8개
- 공유방별 섹션 구분 (색상 점)
- 상단에 완료율 표시 "오늘 3/5 완료"

#### 갱신 정책
- 투두 완료/생성 시: `WidgetCenter.shared.reloadAllTimelines()` 즉시 호출
- 자동 갱신: 자정 (날짜 변경 시)
- App Group을 통해 앱 ↔ 위젯 데이터 공유 (CloudKit 직접 접근 X)

#### 잠금 화면 위젯 (iOS 16+)
- Circular: 오늘 완료율 링 그래프
- Rectangular: 가장 급한 투두 1개 제목

---

### 3.6 달력 / 피드 뷰

**캘린더 탭 구조**
```
[상단] 주간 날짜 스트립 (좌우 스와이프로 주 이동)
[하단] 선택된 날짜의 투두 피드
```

**피드 섹션 순서**
1. 마감 지남 (오늘 이전 미완료, 빨간 배지)
2. 오늘
3. 내일
4. 이번 주

**월간 뷰 (날짜 스트립 길게 탭)**
- 날짜별 dot: 투두 있는 날 표시
- 공유방 색상 dot 여러 개 가능

**알림 배지**
- 앱 아이콘 배지: 오늘 마감 미완료 개수
- 마감 당일 오전 9시 로컬 알림 (알림 권한 있을 때)

---

### 3.7 알림 (Notification)

| 트리거 | 내용 | 타이밍 |
|--------|------|--------|
| 마감일 당일 | "[제목] 오늘 마감이에요" | 오전 9시 |
| 마감일 1일 전 | "[제목] 내일까지예요" | 오전 9시 |
| 연계 해제 | "[제목] 이제 할 수 있어요" | 즉시 |
| 공유방 완료 | "[이름]님이 [제목] 완료했어요" | 즉시 (다른 멤버에게) |

- 알림은 모두 로컬 + CloudKit 푸시 혼합
- 알림 설정: 공유방별 on/off, 종류별 on/off

---

## 4. 비기능 요구사항

### 4.1 성능

| 항목 | 목표 |
|------|------|
| 앱 콜드 스타트 | 1.5초 이내 |
| 투두 생성 응답 | 즉시 (로컬 저장) |
| CloudKit sync 지연 | 온라인 상태에서 5초 이내 |
| 위젯 갱신 반영 | 완료 후 2초 이내 |
| 투두 1,000개 기준 리스트 스크롤 | 60fps 유지 |

### 4.2 오프라인

- 모든 CRUD는 오프라인에서 가능 (SwiftData 로컬)
- 온라인 복구 시 CloudKit 자동 sync
- 오프라인 상태 표시 (상단 배너, 3초 후 자동 숨김)

### 4.3 보안 / 프라이버시

- 데이터는 iCloud 계정에 암호화 저장 (CloudKit 기본 암호화)
- 개인 투두(Space 없음)는 타인이 접근 불가
- 앱 잠금: Face ID / Touch ID 옵션 (설정에서 on/off)
- 분석 데이터 수집 없음 (v1.0)

### 4.4 접근성

- Dynamic Type 지원 (모든 텍스트)
- VoiceOver 레이블 설정
- 고대비 모드 대응
- 최소 탭 영역 44×44pt

---

## 5. UX / 디자인 원칙

### 5.1 핵심 원칙

1. **위젯이 1순위**: 앱을 열지 않아도 완료할 수 있어야 한다.
2. **빠른 추가**: 투두 생성은 2탭 이내. 텍스트 입력 → 완료.
3. **공유는 자연스럽게**: 초대가 번거로우면 공유를 안 한다. 링크 복사 한 번으로 완료.
4. **상태가 보여야 한다**: 잠긴 투두, 내 차례, 공유방 색상 — 한눈에 파악.

### 5.2 주요 UX 플로우

**신규 사용자 온보딩 (3단계)**
```
1. "오늘 할 일 추가해보세요" → 첫 투두 생성 유도
2. "같이 사는 사람 있나요?" → 공유방 생성 유도 (스킵 가능)
3. "위젯을 추가하면 편해요" → 위젯 추가 가이드 (스킵 가능)
```

**투두 빠른 추가 플로우**
```
플로팅 + 버튼 탭
→ 텍스트 필드 포커스 (키보드 자동 올라옴)
→ 제목 입력
→ 하단 툴바: 날짜 / 담당자 / 반복 / 더보기
→ Return 키 → 저장 + 다음 투두 즉시 입력 가능
```

**연계 설정 플로우**
```
투두 상세 → "선행 조건 추가"
→ 같은 Space 투두 검색/선택
→ "[선행 투두] 완료 후 활성화" 확인
→ 저장 → status = locked으로 변경
```

### 5.3 빈 상태 뷰

| 화면 | 빈 상태 메시지 | 액션 |
|------|--------------|------|
| 내 투두 | "오늘 할 일이 없어요" | 투두 추가 버튼 |
| 공유방 투두 | "[이름]방의 첫 할 일을 추가해봐요" | 투두 추가 버튼 |
| 공유방 목록 | "함께할 사람을 초대해보세요" | 공유방 만들기 버튼 |
| 템플릿 | "자주 반복되는 일을 템플릿으로" | 템플릿 만들기 버튼 |

---

## 6. 데이터 모델 요약

```
Space
  ├── id: UUID
  ├── name: String
  ├── colorHex: String
  ├── cloudKitShareID: String?
  └── todos: [TodoItem]

TodoItem
  ├── id: UUID
  ├── title: String
  ├── notes: String?
  ├── dueDate: Date?
  ├── isCompleted: Bool
  ├── completedAt: Date?
  ├── status: locked | available | completed
  ├── assigneeID: String?          ← iCloud User Record ID
  ├── prerequisiteIDs: [UUID]      ← 선행 투두 (관계 대신 UUID로)
  ├── recurrenceData: Data?        ← RecurrenceRule JSON
  ├── templateID: UUID?
  └── space: Space?

TodoTemplate
  ├── id: UUID
  ├── name: String
  └── items: [TemplateItem]        ← JSON 직렬화

WidgetTodo (App Group 경량 모델)
  ├── id: UUID
  ├── title: String
  ├── isCompleted: Bool
  └── dueDate: Date?
```

**설계 결정 근거**
- `prerequisiteIDs`를 UUID 배열로 유지: CloudKit to-many 관계 sync 불안정 → UUID 직접 참조
- `RecurrenceRule`을 Data로: 중첩 Codable 저장, SwiftData 마이그레이션 단순화
- `WidgetTodo` 별도 분리: 위젯 Extension에서 SwiftData 직접 접근 X → App Group JSON

---

## 7. 출시 전략

### 7.1 v1.0 포함 기능 (MVP)

- [x] 투두 CRUD + 완료
- [x] 반복 (매일/매주/완료 후 N일)
- [x] 공유방 (CloudKit)
- [x] 연계형 투두
- [x] 홈 위젯 (Small, Medium)
- [x] 달력/피드 뷰
- [ ] 템플릿 (v1.1로 연기 가능)

### 7.2 v1.1 추가 예정

- 템플릿
- 잠금 화면 위젯
- 공유방 내 댓글
- Large 위젯

### 7.3 App Store 메타데이터

**카테고리**: Productivity
**키워드**: 할일, 투두, 공유, 커플, 가족, 체크리스트, 루틴, 위젯

**스크린샷 순서** (5장)
1. 위젯으로 완료하는 장면
2. 공유방 투두 리스트
3. 연계형 투두 설정
4. 달력 뷰
5. 반복 루틴 설정

---

## 8. 미결 사항 (결정 필요)

| 번호 | 질문 | 옵션 |
|------|------|------|
| Q1 | iCloud 없는 사용자도 앱 사용 가능? | A) 공유 기능만 잠금 B) iCloud 필수 |
| Q2 | 투두 완료 시 완료됨 섹션 보존 기간? | A) 7일 B) 30일 C) 영구 |
| Q3 | 공유방 내 투두 삭제 권한? | A) Owner만 B) 작성자도 가능 |
| Q4 | 알림 기본값? | A) 기본 ON B) 기본 OFF (최초 권한 요청 시 선택) |
| Q5 | 템플릿 커뮤니티 공유 기능? | A) v1.0 포함 B) v2.0으로 연기 |

---

*이 문서는 개발 진행에 따라 지속 업데이트됩니다.*
